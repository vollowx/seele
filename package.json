{
  "name": "@vollowx/seele",
  "version": "0.12.4",
  "description": "Standard Extensible Elements. A web components library that can be styled and extended freely, pre-providing components in Material Design 3.",
  "author": "vollowx",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/all.js",
  "module": "./src/all.js",
  "exports": {
    ".": "./src/all.js",
    "./base/*": "./src/base/*",
    "./m3/*": "./src/m3/*"
  },
  "files": [
    "src/**/*.js",
    "src/**/*.d.ts",
    "custom-elements.json",
    "LICENSE",
    "README.md"
  ],
  "customElements": "custom-elements.json",
  "keywords": [
    "web-components",
    "material-design",
    "material-3",
    "lit",
    "ui-components"
  ],
  "homepage": "https://seele.v9.nz",
  "repository": {
    "type": "git",
    "url": "https://github.com/vollowx/seele.git"
  },
  "scripts": {
    "dev": "wds",
    "dev:styles": "bun scripts/build-styles.ts --watch",
    "build": "bun run build:styles && bun run build:compile",
    "build:compile": "bun scripts/build.ts",
    "build:styles": "bun scripts/build-styles.ts --minify",
    "cleanup": "bun run cleanup:compile && bun run cleanup:styles",
    "cleanup:compile": "bun scripts/cleanup.ts compile",
    "cleanup:styles": "bun scripts/cleanup.ts styles",
    "prepublishOnly": "bun cleanup && bun run build",
    "format": "prettier . --check",
    "format:fix": "prettier . --check --write",
    "format:sortcss": "bunx postcss \"src/**/*.css\" --replace --no-map",
    "cem": "bunx cem analyze --litelement --globs 'src/m3/*.ts' --exclude 'src/m3/*.css.ts'"
  },
  "devDependencies": {
    "@custom-elements-manifest/analyzer": "^0.11.0",
    "@swc/core": "^1.15.18",
    "@types/bun": "^1.3.11",
    "@web/dev-server": "^0.4.6",
    "@web/dev-server-esbuild": "^1.0.5",
    "chokidar": "^5.0.0",
    "lightningcss": "^1.32.0",
    "postcss": "^8.5.8",
    "postcss-cli": "^11.0.1",
    "postcss-sorting": "^9.1.0",
    "prettier": "^3.8.1"
  },
  "dependencies": {
    "@floating-ui/dom": "^1.7.6",
    "@swc/helpers": "^0.5.19",
    "lit": "^3.3.2"
  }
}
